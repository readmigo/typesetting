#include "typesetting/page.h"

namespace typesetting {

// Page is a pure data struct; no additional logic needed currently.
// This file exists to maintain the module structure for future extensions
// (e.g., page serialization, hit testing).

} // namespace typesetting
