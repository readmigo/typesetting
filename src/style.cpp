#include "typesetting/style.h"

namespace typesetting {

// Style is a pure data struct; no additional logic needed currently.
// This file exists to maintain the module structure for future extensions
// (e.g., style inheritance, cascading, validation).

} // namespace typesetting
